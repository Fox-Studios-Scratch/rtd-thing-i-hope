[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Fox Studios - Scratch"
authors = [{name = "Aiden", email = "aiden5488@gmail.com"}]
dynamic = ["version", "description"]
